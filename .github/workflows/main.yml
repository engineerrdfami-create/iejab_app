name: Deploy Najma App Luxury Dark UI
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'

    - name: Create Official Najma Project
      run: |
        flutter create clean_iejab_app --org com.najma.app --project-name iejab_app
        rm -rf clean_iejab_app/lib/*

    - name: Inject Luxury Dark UI Files
      run: |
        cd clean_iejab_app/lib/
        
        # 1. Main entry file
        cat << 'EOF' > main.dart
        import 'package:flutter/material.dart';
        import 'main_navigation.dart';
        void main() => runApp(const MyApp());
        class MyApp extends StatelessWidget {
          const MyApp({super.key});
          @override
          Widget build(BuildContext context) {
            return MaterialApp(
              title: 'نجمة',
              theme: ThemeData.dark().copyWith(
                scaffoldBackgroundColor: const Color(0xFF0F101D),
                primaryColor: const Color(0xFF6C5CE7),
                colorScheme: const ColorScheme.dark(primary: Color(0xFF6C5CE7), secondary: Color(0xFF00CEC9)),
              ),
              debugShowCheckedModeBanner: false,
              home: const MainNavigationScreen(),
            );
          }
        }
        EOF

        # 2. Main Navigation
        cat << 'EOF' > main_navigation.dart
        import 'package:flutter/material.dart';
        import 'star_chat.dart';
        import 'star_live.dart';
        import 'najma_reels.dart';
        import 'star_market.dart';
        import 'starpay_wallet.dart';
        class MainNavigationScreen extends StatefulWidget {
          const MainNavigationScreen({super.key});
          @override
          State<MainNavigationScreen> createState() => _MainNavigationScreenState();
        }
        class _MainNavigationScreenState extends State<MainNavigationScreen> {
          int _currentIndex = 2; 
          final List<Widget> _screens = [
            const StarChatScreen(),
            const StarLiveScreen(),
            const NajmaReelsScreen(),
            const StarMarketScreen(),
            const StarPayWalletScreen(),
          ];
          @override
          Widget build(BuildContext context) {
            return Scaffold(
              body: _screens[_currentIndex],
              bottomNavigationBar: BottomNavigationBar(
                currentIndex: _currentIndex,
                type: BottomNavigationBarType.fixed,
                backgroundColor: const Color(0xFF070814),
                selectedItemColor: const Color(0xFF6C5CE7),
                unselectedItemColor: Colors.grey.shade600,
                onTap: (index) => setState(() => _currentIndex = index),
                items: const [
                  BottomNavigationBarItem(icon: Icon(Icons.chat_bubble_outline), activeIcon: Icon(Icons.chat_bubble), label: 'رسائل'),
                  BottomNavigationBarItem(icon: Icon(Icons.live_tv_outlined), activeIcon: Icon(Icons.live_tv), label: 'بث مباشر'),
                  BottomNavigationBarItem(icon: Icon(Icons.play_circle_outline), activeIcon: Icon(Icons.play_circle_fill), label: 'نجمة'),
                  BottomNavigationBarItem(icon: Icon(Icons.explore_outlined), activeIcon: Icon(Icons.explore), label: 'استكشف'),
                  BottomNavigationBarItem(icon: Icon(Icons.account_balance_wallet_outlined), activeIcon: Icon(Icons.account_balance_wallet), label: 'محفظة'),
                ],
              ),
            );
          }
        }
        EOF

        # 3. Star Chat Screen
        cat << 'EOF' > star_chat.dart
        import 'package:flutter/material.dart';
        class StarChatScreen extends StatelessWidget {
          const StarChatScreen({super.key});
          @override
          Widget build(BuildContext context) {
            return Scaffold(
              appBar: AppBar(
                title: const Text('رسائل', style: TextStyle(fontWeight: FontWeight.bold, color: Colors.white)),
                backgroundColor: const Color(0xFF0F101D),
                elevation: 0,
                actions: [IconButton(icon: const Icon(Icons.edit_note, color: Colors.white), onPressed: () {})],
              ),
              body: ListView.builder(
                itemCount: 5,
                itemBuilder: (context, index) {
                  final names = ['أحمد المقبلي', 'مريم علي', 'فريق الدعم', 'أسامة خالد', 'يسرى محمد'];
                  final msgs = ['مرحباً بك، هل شاهدت البث اليوم؟', 'تم إرسال الهدية بنجاح 🎁', 'كيف يمكننا مساعدتك يا هندسة؟', 'التطبيق فخم جداً عاشت يدك', 'هل رصيد المحفظة محدث؟'];
                  final times = ['١٢:٠١ م', '١١:٤٥ ص', 'أمس', 'أمس', '٢ سبتمبر'];
                  return ListTile(
                    leading: Stack(
                      children: [
                        CircleAvatar(
                          radius: 25,
                          backgroundColor: const Color(0xFF6C5CE7),
                          child: Text(names[index][0], style: const TextStyle(color: Colors.white, fontWeight: FontWeight.bold)),
                        ),
                        if (index < 2)
                          Positioned(bottom: 0, right: 0, child: Container(width: 12, height: 12, decoration: BoxDecoration(color: Colors.green, shape: BoxShape.circle, border: Border.all(color: const Color(0xFF0F101D), width: 2)))),
                      ],
                    ),
                    title: Text(names[index], style: const TextStyle(fontWeight: FontWeight.bold, color: Colors.white)),
                    subtitle: Text(msgs[index], style: TextStyle(color: Colors.grey.shade400, fontSize: 13), maxLines: 1, overflow: TextOverflow.ellipsis),
                    trailing: Text(times[index], style: TextStyle(color: Colors.grey.shade600, fontSize: 11)),
                  );
                },
              ),
            );
          }
        }
        EOF

        # 4. Star Live Screen
        cat << 'EOF' > star_live.dart
        import 'package:flutter/material.dart';
        class StarLiveScreen extends StatelessWidget {
          const StarLiveScreen({super.key});
          @override
          Widget build(BuildContext context) {
            return Scaffold(
              appBar: AppBar(title: const Text('بث مباشر حقيقي', style: TextStyle(fontWeight: FontWeight.bold)), backgroundColor: const Color(0xFF0F101D), elevation: 0),
              body: GridView.builder(
                padding: const EdgeInsets.all(12),
                gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount: 2, crossAxisSpacing: 12, mainAxisSpacing: 12, childAspectRatio: 0.8),
                itemCount: 2,
                itemBuilder: (context, index) {
                  final hosts = ['الشيف جلال', 'لينا أحمد - طبخ'];
                  return Container(
                    decoration: BoxDecoration(
                      color: const Color(0xFF1F2033),
                      borderRadius: BorderRadius.circular(16),
                      image: DecorationImage(
                        image: NetworkImage('https://picsum.photos{index + 10}'),
                        fit: BoxFit.cover,
                        colorFilter: ColorFilter.mode(Colors.black.withOpacity(0.3), BoxBlendMode.srcOver),
                      ),
                    ),
                    child: Stack(
                      children: [
                        Positioned(
                          top: 10, left: 10,
                          child: Container(
                            padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
                            decoration: BoxDecoration(color: Colors.redAccent, borderRadius: BorderRadius.circular(8)),
                            child: const Text('بث مباشر', style: TextStyle(color: Colors.white, fontSize: 11, fontWeight: FontWeight.bold)),
                          ),
                        ),
                        Positioned(
                          bottom: 12, right: 12, left: 12,
                          child: Row(
                            children: [
                              const CircleAvatar(radius: 12, backgroundColor: Color(0xFF6C5CE7), child: Icon(Icons.person, size: 12, color: Colors.white)),
                              const SizedBox(width: 8),
                              Expanded(child: Text(hosts[index], style: const TextStyle(color: Colors.white, fontWeight: FontWeight.bold, fontSize: 12), overflow: TextOverflow.ellipsis)),
                            ],
                          ),
                        )
                      ],
                    ),
                  );
                },
              ),
            );
          }
        }
        EOF

        # 5. Najma Reels Screen
        cat << 'EOF' > najma_reels.dart
        import 'package:flutter/material.dart';
        class NajmaReelsScreen extends StatelessWidget {
          const NajmaReelsScreen({super.key});
          @override
          Widget build(BuildContext context) {
            return Scaffold(
              body: Stack(
                children: [
                  Container(
                    width: double.infinity,
                    height: double.infinity,
                    decoration: const BoxDecoration(
                      image: DecorationImage(
                        image: NetworkImage('https://unsplash.com'),
                        fit: BoxFit.cover,
                      ),
                    ),
                  ),
                  Container(
                    decoration: BoxDecoration(
                      gradient: LinearGradient(
